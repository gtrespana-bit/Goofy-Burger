# --- Vigía: núcleo (obligatorio) -------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.6
opencv-python-headless>=4.9        # si quieres ventanas/UI de OpenCV usa opencv-python
requests>=2.31
apscheduler>=3.10

# --- Opcionales (recomendados) ----------------------------------------------
# ONVIF (autodescubrimiento avanzado, PTZ, perfiles): descomenta para instalar
# onvif-zeep>=0.2.12
# Detección con IA (personas / vehículos / mascotas) vía YOLO:
# ultralytics>=8.1
